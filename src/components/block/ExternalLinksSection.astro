---
import ExternalLinkCard from '@/components/block/ExternalLinkCard.astro';

export interface ExternalLink {
  name: string;
  href: string;
  description: string;
}

interface Props {
  title: string;
  subtitle?: string;
  externalLinks: ExternalLink[];
}

const { 
  title,
  subtitle,
  externalLinks,
} = Astro.props;
---

<section>
    <h2 class="mt-0">{title}</h2>
    <p>
      <slot />
    </p>
    {subtitle && <h3 class="mb-0">{subtitle}</h3>}
    <div id="contributions" class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8 py-6 md:py-8">
        {externalLinks.map((externalLink) => (
            <ExternalLinkCard 
              title={externalLink.name}
              href={externalLink.href}
              description={externalLink.description}
            />
        ))}
    </div>
</section>