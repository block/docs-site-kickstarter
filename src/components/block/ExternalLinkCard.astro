---

import ContentSection from '@/components/block/ContentSection.astro';
import Icon from './Icon.astro';

interface Props {
  title: string;
  href: string;
  description: string;
}

const { 
  title,
  href,
  description
} = Astro.props;
---

<ContentSection
    hasDefaultInnerStyles={false}>
    <a
        href={href}
        target="_blank"
        rel="noopener noreferrer"
        class="group h-full p-6 md:p-8 bg-white flex flex-col text-left rounded-lg no-underline transition-colors duration-200 hover:bg-accent cursor-pointer"
    >
        <div class="flex items-start justify-between mb-3">
            <div class="flex-1">
                <h3 class="text-base md:text-lg lg:text-xl font-semibold mt-0 pt-0">
                    {title}
                </h3>
            </div>
            <div class="flex-shrink-0 ml-2 transition-transform duration-200 group-hover:scale-120">
                <Icon name="external-link" />
            </div>
        </div>
        <div class="flex-1">
            <p class="text-sm md:text-base text-gray-600 dark:text-gray-300 leading-relaxed mt-0">
                {description}
            </p>
        </div>
    </a>
</ContentSection>